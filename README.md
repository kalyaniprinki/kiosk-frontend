# Kiosk Frontend (React + Vite)
Minimal frontend for a printing kiosk with three login types: user, admin, kiosk.

## Run locally
1. `npm install`
2. `npm run dev`
3. Open http://localhost:5173

This is a minimal scaffold: the API calls are mocked inside `src/api/mockApi.js`. Replace with your real API.

C:\Users\kalya\Downloads\project\frontend> git add src
>> git commit -m "Pushed updated src folder"
>> git push -u origin main

1. Initialize Git (only once per project)

If your folder is not yet a Git repo:

git init
2. Add all files
git add .

✅ 3. Commit the changes
git commit -m "Initial commit"

git remote add origin https://github.com/kalyani123/myproject.git

5. Push files to GitHub

If the GitHub repo is new and empty:

git push -u origin main

git add .
git commit -m "update commit1"
git push -u origin main